<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="outlookmAnAger" Height="660" Width="960"
        Background="#1E1E2E" Foreground="#EAEAEF"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="13">

  <Window.Resources>
    <!-- ═══ Color Palette ═══ -->
    <SolidColorBrush x:Key="BgBrush" Color="#1E1E2E"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#2A2A3C"/>
    <SolidColorBrush x:Key="SurfaceHoverBrush" Color="#33334A"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#3E3E55"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#7C6FE0"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#9589E8"/>
    <SolidColorBrush x:Key="AccentSubtleBrush" Color="#2E2B4A"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#EAEAEF"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#9898A8"/>
    <SolidColorBrush x:Key="TextOnAccentBrush" Color="#FFFFFF"/>

    <!-- ═══ Button Style ═══ -->
    <Style TargetType="Button">
      <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextOnAccentBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#6A5DC8"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Secondary button style (surface-colored) -->
    <Style x:Key="SecondaryButton" TargetType="Button">
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceHoverBrush}"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#2A2A3C"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ ListBox Style ═══ -->
    <Style TargetType="ListBox">
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="4"/>
      <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBox">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ScrollViewer Focusable="False" HorizontalScrollBarVisibility="Disabled">
                <ItemsPresenter/>
              </ScrollViewer>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ ListBoxItem Style ═══ -->
    <Style TargetType="ListBoxItem">
      <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
      <Setter Property="Padding" Value="10,8"/>
      <Setter Property="Margin" Value="0,1"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="Bd"
                    Background="Transparent"
                    CornerRadius="4"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceHoverBrush}"/>
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentSubtleBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextOnAccentBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ TabControl Style ═══ -->
    <Style TargetType="TabControl">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabControl">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <!-- Tab strip with bottom border -->
              <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,0">
                <TabPanel IsItemsHost="True" Margin="4,0,0,0"/>
              </Border>
              <!-- Content area -->
              <Border Grid.Row="1" Background="Transparent" Margin="0,8,0,0">
                <ContentPresenter ContentSource="SelectedContent"/>
              </Border>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ TabItem Style ═══ -->
    <Style TargetType="TabItem">
      <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabItem">
            <Border x:Name="Bd" Padding="16,10" Background="Transparent" BorderThickness="0,0,0,2" BorderBrush="Transparent" Margin="0,0,4,0">
              <ContentPresenter ContentSource="Header" HorizontalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource SurfaceHoverBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ TextBox Style ═══ -->
    <Style TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="8,6"/>
      <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ScrollViewer x:Name="PART_ContentHost" Focusable="False"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

  </Window.Resources>

  <Grid Margin="16" ClipToBounds="True">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="12"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- ═══ Header ═══ -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
      <TextBlock Text="outlookmAnAger" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
      <TextBlock Text="Mail Utilities" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,0" VerticalAlignment="Center"/>
    </StackPanel>

    <!-- ═══ Tab Control ═══ -->
    <TabControl Grid.Row="2" Name="MainTabs">

      <!-- ─── Signatures Tab ─── -->
      <TabItem Header="Signatures">
        <Grid Margin="0,8,0,0">
          <Grid.ColumnDefinitions>
            <!-- LEFT: library -->
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="12"/>
            <!-- CENTER: editor + preview -->
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="12"/>
            <!-- RIGHT: assignment grid -->
            <ColumnDefinition Width="300"/>
          </Grid.ColumnDefinitions>

          <!-- ══ LEFT PANEL: Signature Library ══ -->
          <Grid Grid.Column="0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="8"/>
              <RowDefinition Height="*"/>
              <RowDefinition Height="10"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="SIGNATURES" FontWeight="SemiBold" FontSize="11"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="2,0,0,0"/>

            <!-- Signature list with validation indicators -->
            <ListBox Grid.Row="2" Name="LbSignatures" />

            <!-- Action buttons -->
            <Grid Grid.Row="4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="4"/>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>
              <Button Grid.Row="0" Grid.Column="0" Name="BtnNewSignature"       Content="New"       Style="{StaticResource SecondaryButton}" Padding="0,7"/>
              <Button Grid.Row="0" Grid.Column="2" Name="BtnDuplicateSignature" Content="Duplicate" Style="{StaticResource SecondaryButton}" Padding="0,7"/>
              <Button Grid.Row="2" Grid.Column="0" Name="BtnRenameSignature"    Content="Rename"    Style="{StaticResource SecondaryButton}" Padding="0,7"/>
              <Button Grid.Row="2" Grid.Column="2" Name="BtnDeleteSignature"    Content="Delete"    Style="{StaticResource SecondaryButton}" Padding="0,7"/>
            </Grid>
          </Grid>

          <!-- ══ CENTER PANEL: Editor + Preview ══ -->
          <Grid Grid.Column="2">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="8"/>
              <RowDefinition Height="*"/>
              <RowDefinition Height="10"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Signature name + toolbar row -->
            <Grid Grid.Row="0">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Name="TxtEditorTitle"
                         Text="Select a signature"
                         FontSize="14" FontWeight="SemiBold"
                         Foreground="{StaticResource TextPrimaryBrush}"
                         VerticalAlignment="Center"/>
              <Button Grid.Column="2" Name="BtnToggleEditor" Content="Edit HTML"
                      Style="{StaticResource SecondaryButton}" Padding="10,6" FontSize="11"/>
              <Button Grid.Column="4" Name="BtnSaveSignature" Content="Save"
                      Padding="10,6" FontSize="11" IsEnabled="False"/>
              <Button Grid.Column="6" Name="BtnRefreshSignatures" Content="Refresh"
                      Style="{StaticResource SecondaryButton}" Padding="10,6" FontSize="13"/>
            </Grid>

            <!-- Preview / Editor area (toggled) -->
            <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}"
                    CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
              <Grid>
                <!-- WYSIWYG preview via WebBrowser -->
                <!-- Scrollbar hidden via overflow:hidden injected into body on LoadCompleted in Ui.ps1 -->
                <Border Name="PanelPreview" CornerRadius="7" ClipToBounds="True" Background="{StaticResource BgBrush}">
                  <WebBrowser Name="PreviewBrowser"/>
                </Border>
                <!-- Raw HTML editor (hidden by default) -->
                <TextBox Name="TxtHtmlEditor"
                         Visibility="Collapsed"
                         AcceptsReturn="True" AcceptsTab="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="12"
                         Background="{StaticResource BgBrush}"
                         Padding="12"
                         BorderThickness="0"/>
              </Grid>
            </Border>

            <!-- Status / validation line + export/import -->
            <Grid Grid.Row="4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Name="TxtSignatureInfo"
                         Text="Select a signature to edit or preview."
                         Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"
                         VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
              <Button Grid.Column="2" Name="BtnExportSignature" Content="Export"
                      Style="{StaticResource SecondaryButton}" Padding="10,6" FontSize="11"/>
              <Button Grid.Column="4" Name="BtnImportSignature" Content="Import"
                      Style="{StaticResource SecondaryButton}" Padding="10,6" FontSize="11"/>
            </Grid>
          </Grid>

          <!-- ══ RIGHT PANEL: Mailbox Assignment ══ -->
          <Border Grid.Column="4" Background="{StaticResource SurfaceBrush}"
                  CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="12">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>

              <!-- Header -->
              <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Assignment" FontWeight="SemiBold" FontSize="13"
                           Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Name="BtnRefreshAssignments" Content="Refresh"
                        Style="{StaticResource SecondaryButton}" Padding="8,4" FontSize="13"/>
              </Grid>

              <!-- Column headers -->
              <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="6"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="4,0">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="90"/>
                  </Grid.ColumnDefinitions>
                  <TextBlock Grid.Column="0" Text="Account" FontSize="11" FontWeight="SemiBold"
                             Foreground="{StaticResource TextSecondaryBrush}"/>
                  <TextBlock Grid.Column="2" Text="New mail" FontSize="11" FontWeight="SemiBold"
                             Foreground="{StaticResource TextSecondaryBrush}"/>
                  <TextBlock Grid.Column="4" Text="Reply" FontSize="11" FontWeight="SemiBold"
                             Foreground="{StaticResource TextSecondaryBrush}"/>
                </Grid>

                <!-- Assignment rows scroll container -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                  <StackPanel Name="PanelAssignmentRows"/>
                </ScrollViewer>
              </Grid>

              <!-- Apply button + Outlook warning -->
              <Grid Grid.Row="4">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="6"/>
                  <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Name="BtnApplyAssignments" Content="Apply to Outlook"
                        HorizontalAlignment="Stretch"/>
                <TextBlock Grid.Row="2" Name="TxtOutlookWarning"
                           Text="[!] Outlook is running - restart to apply changes."
                           Foreground="#E0A84A" FontSize="11" Visibility="Collapsed"
                           TextWrapping="Wrap"/>
              </Grid>
            </Grid>
          </Border>
        </Grid>
      </TabItem>

      <!-- ─── Permissions Tab ─── -->
      <TabItem Header="Permissions">
        <Grid Margin="0,8,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <!-- Left panel: accounts list -->
          <Grid Grid.Column="0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="8"/>
              <RowDefinition Height="*"/>
              <RowDefinition Height="12"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Signed-in Accounts" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="2,0,0,0"/>
            <ListBox Grid.Row="2" Name="LbAccounts"/>
            <Button Grid.Row="4" Name="BtnRefreshAccounts" Content="Refresh" Width="80" HorizontalAlignment="Left" Style="{StaticResource SecondaryButton}"/>
          </Grid>

          <!-- Right panel: permissions controls -->
          <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="16">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>

              <TextBlock Grid.Row="0" Text="Calendar Permissions" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}"/>

              <!-- Input: target user -->
              <StackPanel Grid.Row="2" Orientation="Vertical">
                <TextBlock Text="Target user or email" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                <TextBox Name="TxtPermissionUser" Background="{StaticResource BgBrush}"/>
              </StackPanel>

              <!-- Permission level selector + action -->
              <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="12"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical">
                  <TextBlock Text="Permission level" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                  <ListBox Name="CbPermissionLevel" Background="{StaticResource BgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Height="Auto">
                    <ListBox.ItemsPanel>
                      <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                      </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBoxItem Content="Can read" Padding="12,6" Margin="0,0,4,0"/>
                    <ListBoxItem Content="Can read &amp; move/delete" Padding="12,6" Margin="0,0,4,0"/>
                    <ListBoxItem Content="Can reply" Padding="12,6"/>
                  </ListBox>
                </StackPanel>

                <Button Grid.Column="2" Name="BtnSetPermission" Content="Set" Width="80" VerticalAlignment="Bottom"/>
              </Grid>

              <TextBlock Grid.Row="6" Text="Current permissions" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
              <ListBox Grid.Row="8" Name="LbPermissions"/>
            </Grid>
          </Border>
        </Grid>
      </TabItem>

      <!-- ─── Extras Tab ─── -->
      <TabItem Header="Extras">
        <Grid Margin="16">
          <TextBlock Text="Coming soon." Foreground="{StaticResource TextSecondaryBrush}" FontStyle="Italic" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14"/>
        </Grid>
      </TabItem>
    </TabControl>

    <!-- ═══ Status Bar ═══ -->
    <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" CornerRadius="6" Padding="12,6" Margin="0,12,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
      <TextBlock Name="TxtStatus" Text="Ready" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
    </Border>
  </Grid>
</Window>
