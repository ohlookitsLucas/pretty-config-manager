<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pretty Configuration Manager" Height="740" Width="1100"
        Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="13">

  <Window.Resources>
    <!-- ═══ Color Palette ═══ -->
    <SolidColorBrush x:Key="BgBrush" Color="#1E1E2E"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#2A2A3C"/>
    <SolidColorBrush x:Key="SurfaceHoverBrush" Color="#33334A"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#3E3E55"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#7C6FE0"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#9589E8"/>
    <SolidColorBrush x:Key="AccentSubtleBrush" Color="#2E2B4A"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#EAEAEF"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#9898A8"/>
    <SolidColorBrush x:Key="TextOnAccentBrush" Color="#FFFFFF"/>

    <!-- ═══ Button Style ═══ -->
    <Style TargetType="Button">
      <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
      <Setter Property="Foreground" Value="{DynamicResource TextOnAccentBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSubtleBrush}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Secondary button style (surface-colored) -->
    <Style x:Key="SecondaryButton" TargetType="Button">
      <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSubtleBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ ListBox Style ═══ -->
    <Style TargetType="ListBox">
      <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="4"/>
      <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBox">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ScrollViewer Focusable="False" HorizontalScrollBarVisibility="Disabled">
                <ItemsPresenter/>
              </ScrollViewer>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ ListBoxItem Style ═══ -->
    <Style TargetType="ListBoxItem">
      <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
      <Setter Property="Padding" Value="10,8"/>
      <Setter Property="Margin" Value="0,1"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="Bd"
                    Background="Transparent"
                    CornerRadius="4"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSubtleBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ TabControl Style ═══ -->
    <Style TargetType="TabControl">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabControl">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <!-- Tab strip with bottom border -->
              <Border Grid.Row="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,0">
                <TabPanel IsItemsHost="True" Margin="4,0,0,0"/>
              </Border>
              <!-- Content area -->
              <Border Grid.Row="1" Background="Transparent" Margin="0,8,0,0">
                <ContentPresenter ContentSource="SelectedContent"/>
              </Border>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ TabItem Style ═══ -->
    <Style TargetType="TabItem">
      <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabItem">
            <Border x:Name="Bd" Padding="16,10" Background="Transparent" BorderThickness="0,0,0,2" BorderBrush="Transparent" Margin="0,0,4,0">
              <ContentPresenter ContentSource="Header" HorizontalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource SurfaceHoverBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ═══ TextBox Style ═══ -->
    <Style TargetType="TextBox">
      <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="8,6"/>
      <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryBrush}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ScrollViewer x:Name="PART_ContentHost" Focusable="False"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

  </Window.Resources>

  <Grid Margin="16" ClipToBounds="True">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="12"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- ═══ Header ═══ -->
    <StackPanel Grid.Row="0" Orientation="Vertical" VerticalAlignment="Center">
      <TextBlock Text="Pretty Configuration Manager" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
      <TextBlock Text="Mail Utilities" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0"/>
    </StackPanel>

    <!-- ═══ Tab Control ═══ -->
    <TabControl Grid.Row="2" Name="MainTabs">

      <!-- ─── Signatures Tab ─── -->
      <TabItem Header="Signatures" Name="TabSignatures">
        <Grid Margin="0,8,0,0">
          <Grid.ColumnDefinitions>
            <!-- LEFT: library -->
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="12"/>
            <!-- CENTER: editor + preview -->
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="12"/>
            <!-- RIGHT: assignment grid -->
            <ColumnDefinition Width="300"/>
          </Grid.ColumnDefinitions>

          <!-- ══ LEFT PANEL: Signature Library ══ -->
          <Grid Grid.Column="0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="8"/>
              <RowDefinition Height="*"/>
              <RowDefinition Height="10"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="SIGNATURES" FontWeight="SemiBold" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="2,0,0,0"/>

            <!-- Signature list with validation indicators -->
            <ListBox Grid.Row="2" Name="LbSignatures" />

            <!-- Action buttons -->
            <Grid Grid.Row="4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="4"/>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>
              <Button Grid.Row="0" Grid.Column="0" Name="BtnNewSignature"       Content="New"       Style="{DynamicResource SecondaryButton}" Padding="0,7"/>
              <Button Grid.Row="0" Grid.Column="2" Name="BtnDuplicateSignature" Content="Duplicate" Style="{DynamicResource SecondaryButton}" Padding="0,7"/>
              <Button Grid.Row="2" Grid.Column="0" Name="BtnRenameSignature"    Content="Rename"    Style="{DynamicResource SecondaryButton}" Padding="0,7"/>
              <Button Grid.Row="2" Grid.Column="2" Name="BtnDeleteSignature"    Content="Delete"    Style="{DynamicResource SecondaryButton}" Padding="0,7"/>
            </Grid>
          </Grid>

          <!-- ══ CENTER PANEL: Editor + Preview ══ -->
          <Grid Grid.Column="2">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="8"/>
              <RowDefinition Height="*"/>
              <RowDefinition Height="10"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Signature name + toolbar row -->
            <Grid Grid.Row="0">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Name="TxtEditorTitle"
                         Text="Select a signature"
                         FontSize="14" FontWeight="SemiBold"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         VerticalAlignment="Center"/>
              <Button Grid.Column="2" Name="BtnToggleEditor" Content="Edit HTML"
                      Style="{DynamicResource SecondaryButton}" Padding="10,6" FontSize="11"/>
              <Button Grid.Column="4" Name="BtnSaveSignature" Content="Save"
                      Padding="10,6" FontSize="11" IsEnabled="False"/>
              <Button Grid.Column="6" Name="BtnRefreshSignatures" Content="Refresh"
                      Style="{DynamicResource SecondaryButton}" Padding="10,6" FontSize="13"/>
            </Grid>

            <!-- Preview / Editor area (toggled) -->
            <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}"
                    CornerRadius="8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
              <Grid>
                <!-- WYSIWYG preview via WebBrowser -->
                <!-- Scrollbar hidden via overflow:hidden injected into body on LoadCompleted in Ui.ps1 -->
                <Border Name="PanelPreview" CornerRadius="7" ClipToBounds="True" Background="{DynamicResource BgBrush}">
                  <WebBrowser Name="PreviewBrowser" Visibility="Collapsed"/>
                </Border>
                <!-- Raw HTML editor (hidden by default) -->
                <TextBox Name="TxtHtmlEditor"
                         Visibility="Collapsed"
                         AcceptsReturn="True" AcceptsTab="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="12"
                         Background="{DynamicResource BgBrush}"
                         Padding="12"
                         BorderThickness="0"/>
              </Grid>
            </Border>

            <!-- Status / validation line + export/import -->
            <Grid Grid.Row="4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Name="TxtSignatureInfo"
                         Text="Select a signature to edit or preview."
                         Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"
                         VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
              <Button Grid.Column="2" Name="BtnExportSignature" Content="Export"
                      Style="{DynamicResource SecondaryButton}" Padding="10,6" FontSize="11"/>
              <Button Grid.Column="4" Name="BtnImportSignature" Content="Import"
                      Style="{DynamicResource SecondaryButton}" Padding="10,6" FontSize="11"/>
            </Grid>
          </Grid>

          <!-- ══ RIGHT PANEL: Mailbox Assignment ══ -->
          <Border Grid.Column="4" Background="{DynamicResource SurfaceBrush}"
                  CornerRadius="8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="12">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>

              <!-- Header -->
              <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Assignment" FontWeight="SemiBold" FontSize="13"
                           Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Name="BtnRefreshAssignments" Content="Refresh"
                        Style="{DynamicResource SecondaryButton}" Padding="8,4" FontSize="13"/>
              </Grid>

              <!-- Column headers -->
              <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="6"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="4,0">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="90"/>
                  </Grid.ColumnDefinitions>
                  <TextBlock Grid.Column="0" Text="Account" FontSize="11" FontWeight="SemiBold"
                             Foreground="{DynamicResource TextSecondaryBrush}"/>
                  <TextBlock Grid.Column="2" Text="New mail" FontSize="11" FontWeight="SemiBold"
                             Foreground="{DynamicResource TextSecondaryBrush}"/>
                  <TextBlock Grid.Column="4" Text="Reply" FontSize="11" FontWeight="SemiBold"
                             Foreground="{DynamicResource TextSecondaryBrush}"/>
                </Grid>

                <!-- Assignment rows scroll container -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                  <StackPanel Name="PanelAssignmentRows"/>
                </ScrollViewer>
              </Grid>

              <!-- Apply button + Outlook warning -->
              <Grid Grid.Row="4">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="6"/>
                  <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Name="BtnApplyAssignments" Content="Apply to Outlook"
                        HorizontalAlignment="Stretch"/>
                <TextBlock Grid.Row="2" Name="TxtOutlookWarning"
                           Text="[!] Outlook is running - restart to apply changes."
                           Foreground="#E0A84A" FontSize="11" Visibility="Collapsed"
                           TextWrapping="Wrap"/>
              </Grid>
            </Grid>
          </Border>
        </Grid>
      </TabItem>

      <!-- ─── Permissions Tab ─── -->
      <TabItem Header="Permissions" Name="TabPermissions">
        <Grid Margin="0,8,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <!-- ── Column 0: Mailbox list ── -->
          <Grid Grid.Column="0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="6"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="6"/>
              <RowDefinition Height="*"/>
              <RowDefinition Height="10"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="MAILBOXES" FontWeight="SemiBold" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="2,0,0,0"/>

            <!-- Filter box -->
            <TextBox Grid.Row="2" Name="TbMailboxSearch"
                     ToolTip="Type to filter the list below"/>

            <ListBox Grid.Row="4" Name="LbMailboxes">
              <ListBox.ItemTemplate>
                <DataTemplate>
                  <StackPanel Margin="0,2,0,2">
                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"
                               Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding SmtpAddress}" FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis"/>
                  </StackPanel>
                </DataTemplate>
              </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="6" Name="BtnRefreshPerm" Content="Refresh mailboxes"
                    HorizontalAlignment="Stretch" Style="{DynamicResource SecondaryButton}"/>
          </Grid>

          <!-- ── Column 2: Folder list ── -->
          <Grid Grid.Column="2">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="6"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="FOLDERS" FontWeight="SemiBold" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="2,0,0,0"/>

            <ListBox Grid.Row="2" Name="LbFolders">
              <ListBox.ItemTemplate>
                <DataTemplate>
                  <TextBlock TextTrimming="CharacterEllipsis">
                    <TextBlock.Text>
                      <MultiBinding StringFormat="{}{0}{1}">
                        <Binding Path="Indent"/>
                        <Binding Path="Name"/>
                      </MultiBinding>
                    </TextBlock.Text>
                  </TextBlock>
                </DataTemplate>
              </ListBox.ItemTemplate>
            </ListBox>
          </Grid>

          <!-- ── Column 4: People & permissions ── -->
          <Grid Grid.Column="4">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="10"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="6"/>
              <RowDefinition Height="*" MinHeight="80"/>
              <RowDefinition Height="14"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="10"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="8"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="8"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="10"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Section: who has access -->
            <TextBlock Grid.Row="0" Text="WHO HAS ACCESS" FontWeight="SemiBold" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>

            <TextBlock Grid.Row="2" Name="TxtFolderHint"
                       Text="← Select a folder to see who has access"
                       Foreground="{DynamicResource TextSecondaryBrush}" FontStyle="Italic" FontSize="12"/>

            <!-- Current permissions DataGrid -->
            <DataGrid Grid.Row="4" Name="DgCurrentPerms"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      CanUserReorderColumns="False"
                      CanUserSortColumns="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="None"
                      Background="{DynamicResource SurfaceBrush}"
                      Foreground="{DynamicResource TextPrimaryBrush}"
                      BorderBrush="{DynamicResource BorderBrush}"
                      BorderThickness="1"
                      RowBackground="{DynamicResource SurfaceBrush}"
                      AlternatingRowBackground="{DynamicResource BgBrush}"
                      HorizontalScrollBarVisibility="Disabled"
                      ColumnHeaderHeight="30">
              <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                  <Setter Property="Background" Value="{DynamicResource BgBrush}"/>
                  <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                  <Setter Property="FontSize" Value="11"/>
                  <Setter Property="FontWeight" Value="SemiBold"/>
                  <Setter Property="Padding" Value="10,6"/>
                  <Setter Property="BorderThickness" Value="0"/>
                </Style>
              </DataGrid.ColumnHeaderStyle>
              <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                  <Setter Property="BorderThickness" Value="0"/>
                  <Setter Property="Padding" Value="10,6"/>
                  <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                  <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                      <Setter Property="Background" Value="{DynamicResource AccentSubtleBrush}"/>
                      <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                    </Trigger>
                  </Style.Triggers>
                </Style>
              </DataGrid.CellStyle>
              <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                  <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                      <Setter Property="Background" Value="{DynamicResource AccentSubtleBrush}"/>
                    </Trigger>
                  </Style.Triggers>
                </Style>
              </DataGrid.RowStyle>
              <DataGrid.Columns>
                <DataGridTextColumn Header="Person or group" Binding="{Binding User}" Width="*"/>
                <DataGridTextColumn Header="Access level"    Binding="{Binding PermissionLevelName}" Width="180"/>
              </DataGrid.Columns>
            </DataGrid>

            <!-- Divider label -->
            <TextBlock Grid.Row="6" Text="ADD OR CHANGE ACCESS" FontWeight="SemiBold" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>

            <!-- AD search box (with inline popup) -->
            <Grid Grid.Row="8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <Grid Grid.Column="0">
                <TextBox Name="TxtAddUser"
                         ToolTip="Search Active Directory or type an email address directly"/>
                <!-- AD results dropdown -->
                <Popup Name="PopAddUser" PlacementTarget="{Binding ElementName=TxtAddUser}"
                       Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                  <Border Background="{DynamicResource SurfaceBrush}"
                          BorderBrush="{DynamicResource AccentBrush}"
                          BorderThickness="1" CornerRadius="6">
                    <ListBox Name="LbAdResults" MaxHeight="180" BorderThickness="0"
                             Background="{DynamicResource SurfaceBrush}">
                      <ListBox.ItemTemplate>
                        <DataTemplate>
                          <StackPanel Margin="0,2">
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <TextBlock Text="{Binding Mail}" FontSize="11"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                          </StackPanel>
                        </DataTemplate>
                      </ListBox.ItemTemplate>
                    </ListBox>
                  </Border>
                </Popup>
              </Grid>
              <TextBlock Grid.Column="2" Text="🔍" FontSize="16" VerticalAlignment="Center"
                         Foreground="{DynamicResource TextSecondaryBrush}"/>
            </Grid>

            <!-- Permission level ComboBox -->
            <ComboBox Grid.Row="10" Name="CbPermLevel"
                      Background="{DynamicResource SurfaceBrush}"
                      Foreground="{DynamicResource TextPrimaryBrush}"
                      BorderBrush="{DynamicResource BorderBrush}"
                      BorderThickness="1"
                      Padding="8,6"
                      ToolTip="Choose what this person is allowed to do"/>

            <!-- Action buttons -->
            <StackPanel Grid.Row="12" Orientation="Horizontal">
              <Button Name="BtnSavePerm"   Content="Save"   Margin="0,0,8,0" Width="90"/>
              <Button Name="BtnRemovePerm" Content="Remove" Width="90"
                      Style="{DynamicResource SecondaryButton}"/>
            </StackPanel>

            <!-- Inline status / error -->
            <TextBlock Grid.Row="14" Name="TxtPermStatus" FontSize="12"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       TextWrapping="Wrap"/>
          </Grid>
        </Grid>
      </TabItem>

      <!-- ─── Settings Tab ─── -->
      <TabItem Header="Settings" Name="TabSettings">
        <Grid Margin="0,8,0,0">
          <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <!-- Scrollable settings sections -->
          <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="0,0,16,0" MaxWidth="560">

              <!-- ══ Appearance ══ -->
              <TextBlock Text="APPEARANCE" FontWeight="SemiBold" FontSize="11"
                         Foreground="{DynamicResource TextSecondaryBrush}" Margin="2,0,0,12"/>
              <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="8"
                      BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                      Padding="16" Margin="0,0,0,20">
                <StackPanel>
                  <TextBlock Text="Colour theme" FontSize="13" FontWeight="SemiBold"
                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                  <TextBlock Text="Changes take effect immediately." FontSize="11"
                             Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3,0,10"/>
                  <ListBox Name="LbThemeSelector" Background="Transparent" BorderThickness="0" Padding="0">
                    <ListBox.ItemsPanel>
                      <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                      </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBoxItem Content="Dark"  Tag="Dark"  Padding="18,8" Margin="0,0,6,0"/>
                    <ListBoxItem Content="Light" Tag="Light" Padding="18,8" Margin="0,0,6,0"/>
                    <!-- Future themes: add ListBoxItem elements here -->
                  </ListBox>
                </StackPanel>
              </Border>

              <!-- ══ Language ══ -->
              <TextBlock Text="LANGUAGE" FontWeight="SemiBold" FontSize="11"
                         Foreground="{DynamicResource TextSecondaryBrush}" Margin="2,0,0,12"/>
              <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="8"
                      BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                      Padding="16" Margin="0,0,0,20">
                <StackPanel>
                  <TextBlock Text="Application language" FontSize="13" FontWeight="SemiBold"
                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                  <TextBlock Text="Restart required to apply language changes." FontSize="11"
                             Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3,0,10"/>
                  <ListBox Name="LbLanguageSelector" Background="Transparent" BorderThickness="0" Padding="0">
                    <ListBox.ItemsPanel>
                      <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                      </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBoxItem Content="English" Tag="en" Padding="18,8" Margin="0,0,6,0"/>
                    <!-- Future languages: add ListBoxItem elements here -->
                  </ListBox>
                </StackPanel>
              </Border>

            </StackPanel>
          </ScrollViewer>

          <!-- About pinned to bottom -->
          <Border Name="AboutBorder" Grid.Row="1" Padding="16,12" Margin="0,8,16,0" Cursor="Hand">
            <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Text="Inspired by G.Q. and B.B., made by Lucas and Claude."/>
          </Border>

        </Grid>
      </TabItem>

      <!-- ─── Extras Tab ─── -->
      <TabItem Header="Extras" Name="TabExtras" Visibility="Collapsed">
        <Grid Margin="16">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

          <Grid Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Troll Tools" FontSize="15" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
              <Button Name="BtnKillAll" Content="&#x1F480;  Kill All"
                      Style="{DynamicResource SecondaryButton}" Padding="10,5" FontSize="11" Margin="0,0,6,0"/>
              <Button Name="BtnLockExtras" Content="&#x1F512;  Lock"
                      Style="{DynamicResource SecondaryButton}" Padding="10,5" FontSize="11"/>
            </StackPanel>
          </Grid>

          <!-- Scrollable list of extras -->
          <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Name="PanelExtras" Orientation="Vertical"/>
          </ScrollViewer>
        </Grid>
      </TabItem>
    </TabControl>

    <!-- ═══ Status Bar ═══ -->
    <Border Name="StatusBar" Grid.Row="3" Background="{DynamicResource SurfaceBrush}" CornerRadius="6" Padding="12,6" Margin="0,12,0,0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Visibility="Visible">
      <TextBlock Name="TxtStatus" Text="Ready" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
    </Border>
  </Grid>
</Window>
